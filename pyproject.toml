[project]
name = "lfm-vla-libero"
version = "0.1.0"
description = "VLA policy training and evaluation on CALVIN"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "torch",
    "transformers",
    "numpy",
    "pillow",
    "tqdm",
    "matplotlib",
    "pandas",
]
